{% extends 'base.html.twig' %}

{% block body %}
    <h1>Accueil</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accueil.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ accueil.picture }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ accueil.note }}</td>
            </tr>
            <tr>
                <th>Posteur</th>
                <td>{{ accueil.posteur }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('accueil_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('accueil_edit', { 'id': accueil.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
